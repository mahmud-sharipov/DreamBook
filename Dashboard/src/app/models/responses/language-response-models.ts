export interface LanguageResponseModel {
    guid: string;
    name: string;
    code: string;
    isDefault: boolean;
}

export interface LanguageShortResponseModel {
    guid: string;
    name: string;
}